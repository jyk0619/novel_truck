import 'package:flutter/material.dart';

class AppColors {
  //기본색상
  static const primary = Color(0xFFEE7600);
  static const secondary = Color(0xFF03DAC6);
  static const surface = Color(0xFFFFFFFF); // 카드 등의 표면 색상
  static const background = Color(0xFFF5F5F5);

  //텍스트 색상
  static const textPrimary = Color(0xFF212121);
  static const textSecondary = Color(0xFF757575);
  static const textWhite = Color(0xFFFFFFFF); // 밝은 텍스트

  // 기능성 색상
  static const error = Color(0xFFB00020); // 에러/삭제
  static const success = Color(0xFF4CAF50); // 성공
  static const divider = Color(0xFFE0E0E0); // 구분선
}
